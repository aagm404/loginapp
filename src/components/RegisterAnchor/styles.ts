import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    
    registerAnchorText: {
        color: 'black',
        textAlign: 'center',
        marginTop: 20,
        fontSize: 12,
        fontWeight: '700'
    },
});